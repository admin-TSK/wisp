# Copy to ~/.wisp-pilot/pilot.env (optional). Sourced by pilot_verify.sh only.
# Never commit real keys.

# Required for upstream + compression checks in pilot_verify.sh
ANTHROPIC_API_KEY=sk-ant-...

# Only needed for a fresh pilot_local.sh enrol (not for verify if profile.json exists)
# WISP_SAAS_ENDPOINT=https://saas-tau-teal.vercel.app
# WISP_TENANT_ID=00000000-0000-4000-8000-000000000001
# WISP_ENROL_SECRET=your-secret
